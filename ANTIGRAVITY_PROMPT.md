# برومبت جاهز لإرساله إلى Antigravity

أنت تعمل على مشروع Next.js باسم `multiple-intelligences-pro` لنظام اختبار الذكاءات المتعددة باللغة العربية.
أريدك أن تفهم المشروع كاملًا ثم تطوره وتراجعه كنسخة Production Ready.

## المطلوب الأساسي

1. راجع هيكلة المشروع كاملة:
   - Next.js App Router
   - TypeScript
   - Tailwind CSS
   - Prisma
   - SQLite3
   - صفحة الطالب
   - صفحة النتائج
   - لوحة الإدارة
   - تقارير الإدارة
   - تصدير Excel

2. اربط المشروع بقاعدة بيانات SQLite3 محليًا باستخدام Prisma:
   - استخدم `DATABASE_URL="file:./dev.db"`
   - تأكد من وجود `prisma/schema.prisma`
   - شغّل أو جهّز أوامر:
     - `npm run db:push`
     - `npm run db:seed`
   - تأكد أن الجداول هي:
     - Student
     - Result
     - AdminUser

3. تأكد أن تسجيل دخول الإدارة يعمل:
   - صفحة `/admin/login`
   - Cookie آمن HttpOnly
   - بيانات الإدارة من `.env`
   - لا تسمح بالدخول إلى `/admin` و`/admin/reports` و`/admin/results` إلا بعد تسجيل الدخول.

4. حسّن واجهة الطالب إن احتاجت:
   - تصميم عربي RTL حديث
   - تجربة استخدام واضحة
   - Progress bar
   - منع إرسال الاختبار إلا بعد اكتمال الأسئلة
   - تصميم مناسب للطلاب

5. حسّن صفحة النتائج:
   - أعلى 3 ذكاءات
   - الترتيب الكامل للذكاءات
   - نسبة كل ذكاء
   - توصيف واضح لكل ذكاء
   - أنشطة مقترحة
   - واجهة قابلة للطباعة قدر الإمكان

6. حسّن لوحة الإدارة:
   - Cards للإحصائيات
   - عدد الطلاب
   - عدد الاختبارات
   - أكثر ذكاء انتشارًا
   - آخر النتائج
   - روابط واضحة للتقارير والتصدير

7. حسّن صفحة التقارير:
   - توزيع الذكاءات
   - متوسط نسب الذكاءات
   - أكثر ذكاء انتشارًا
   - عرض بصري واضح بدون مكتبات رسوم ثقيلة إن لم تكن ضرورية

8. حسّن تصدير Excel:
   - ملف XLSX منظم
   - ورقة نتائج الطلاب
   - ورقة ملخص
   - أعمدة عربية
   - أعلى 3 ذكاءات
   - نسب جميع الذكاءات
   - تاريخ الاختبار

## مهم جدًا بخصوص Vercel و SQLite3

أريد SQLite3 للتطوير المحلي. لكن عند النشر على Vercel يجب الانتباه أن SQLite كملف محلي لا يحفظ الكتابة بشكل دائم في بيئة Serverless.
لذلك:
- اجعل المشروع يعمل محليًا على SQLite3 عبر Prisma.
- إن كان مطلوبًا تشغيله على Vercel ببيانات دائمة،اقترح أو نفّذ خيار SQLite-compatible مثل Turso/libSQL،أو اشرح بوضوح أن SQLite المحلي على Vercel غير مناسب للإنتاج الدائم.
- لا تكسر تشغيل المشروع المحلي.

## المطلوب في النهاية

- تأكد أن `npm install` يعمل.
- تأكد أن `npm run db:push` يعمل.
- تأكد أن `npm run db:seed` يعمل.
- تأكد أن `npm run dev` يعمل.
- تأكد أن `npm run build` لا يعطي أخطاء TypeScript.
- نظّف الكود من أي ملفات غير مستخدمة.
- لا تغيّر اللغة العربية ولا اتجاه RTL.
- حافظ على التصميم الحديث والبسيط.
